<header id="Init">
	<div>
		<h2>De idea a proyecto</h2>
		<h2><string>impecable</string></h2>
		<p>Comunicanos tus ideas para poder relizarlas. Somos muy eficaces y actualizados.</p>
		<div>
			<button class='btn-primary'><a target="_blank" href="https://cal.com/webmasterl">Reunete con nosotros</a></button>
			<button class='btn-secondary'><a href="mailto:webmasterl@webmasterl.es">Contacto</a></button>
		</div>
	</div>
</header>

<style>
	header {
		width: 100%;
		min-height: calc(100vh - 4rem);
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
		color: var(--white);
		text-align: center;
		padding: 0 1rem;
		text-shadow: 0 0 0.5rem rgba(0, 0, 0, 0.2);
	}

	

	header > div {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		width: fit-content;
		max-width: 50rem;
	}
	h2 {
		font-size: 2.5em;
	}
	p {
		margin: 1.5rem 0;
		font-size: 1em;
	}
	header > div > div {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 3rem;
		width: fit-content;
		margin: 0 auto;
	}
	button {
		background-color: var(--primary);
		color: var(--white);
		padding: 1rem 2rem;
		border: none;
		cursor: pointer;
		
		font-size: 1em;
		border-radius: 1rem;
		transition: background-color 0.2s ease;
		text-decoration: none;
	}
	.btn-primary {
		background-color: #325883;
		box-shadow: 0 0 0.5rem rgba(255, 255, 255, 0.2);
	}
	.btn-primary:hover {
		background-color: #4a66c2;
	}
	.btn-secondary {
		background-color: transparent;
		box-shadow: 0 0 4rem rgba(255, 255, 255, 0.171);
	}
	.btn-secondary:hover {
		background-color: #4c67b913;
	}
	a {
		text-decoration: none;
		color: #FFF;
	}


	@media screen and (min-width: 48rem) {
		header {
			font-size: 1.5em;
		}
	}
</style>
